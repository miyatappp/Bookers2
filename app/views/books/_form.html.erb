<h2>New book</h2>
<%= form_with model: book do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>Opinion </h4>
  <%= f.text_area :body %>
  <div class="text"> 
    <%= f.submit 'Create Book', class: "btn btn-success" %>
  </div>
<% end %>
